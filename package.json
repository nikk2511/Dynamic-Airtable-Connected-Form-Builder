{
  "name": "airtable-form-builder",
  "version": "1.0.0",
  "description": "A full-stack MERN application for building dynamic forms connected to Airtable",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm ci && npm run build",
    "build:vercel": "cd frontend && npm ci && npm run build",
    "build:render": "cd backend && npm install",
    "start": "cd backend && npm start",
    "start:backend": "cd backend && npm start",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["mern", "airtable", "form-builder", "react", "express", "mongodb"],
  "author": "BustBrain Labs",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
